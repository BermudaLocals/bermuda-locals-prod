[[phases.setup]]
nixPkgs = ["python3", "python311", "php", "nodejs"]

[[phases.build]]
commands = ["echo Building..."]

[[phases.install]]
cmds = ["pip install -r requirements.txt"]

[[phases.start]]
cmd = ["python3", "-m", "http.server", "$PORT", "--bind", "0.0.0.0"]